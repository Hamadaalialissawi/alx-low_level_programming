#include "main.h"

/**
  * mul -  function that multiplies two integers.
  *
  * @a: the first number
  * @b: the scund number
  * Return: result
  */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
