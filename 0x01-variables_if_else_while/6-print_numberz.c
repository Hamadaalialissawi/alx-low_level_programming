#include <stdio.h>

/**
  * main - print digit
  *
  * print single digit of base 10
  * Return: usually (0)
  */
int main(void)
{
	int n;

	for (n = 48; n <= 57; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
